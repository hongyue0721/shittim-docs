| Task/Action状态机 | active合同：root v2/child Action与Approval v2；`V2InitialBuildActive`（ADR-0009） | 首批TaskCreate/Child proposal/allocation、ActionRequestV2、ActionTransitionIntentV1、Approval核心五Schema及身份/证据八Schema/generated root types已落地 | root TaskCreate v2 repository、Action current-snapshot/transition/`action.state_changed`、PolicyRule/PD评估编排（PD 原子绑定至 migration 0009）、Approval/Identity/`approval.state_changed`、**Action Lease 全链 owner**（acquire/dispatch/release + Stop owner）均已落地；`domain-task` Lease release effect 与 migration 0010 Lease/Resource Lock/Stop Fence 持久化基座已落地并独立终验 GO；`domain-task` 另已落地 Approval v2 resolution 领域表达（IC §6.5：可消费 current resolution 满足 confirmation 后 `pending→approved`，`ActionEvidence` / `PolicyEvaluationOutcome` / `ActionEventIntent` 携带 `approval_resolution_ref`，deny/confirm 携带 fail closed，resolution⇒`permission_decision_ref` Schema 不变量）；`begin_dispatch`（leased→in_flight，保留双源 Lease）与 `release_or_expire_lease`（三条 leased 退出边 + 三条 in_flight 退出边，消费 typed `LeaseReleaseEffect`，事件 reason_code 闭集）均已落地，`approved→leased→in_flight→completed` 端到端可走通；Stop owner `activate_stop_fence` / `get_stop_fence` 已落地（单事务 Fence 单例 + `stop_fence.activated` global 事件 + `leased→cancelled`/`in_flight→unknown_side_effect` 分流收敛，动态 allocation 事务内产出并入命令唯一 collector，重复激活幂等 readback）；**切片5 child materializer 已落地**（migration 0011：原子物化 bundle + mapping 闭包五钉 FK + 严格 readback 含确定性重建比对 + approval resolution 消费全链，独立审查三轮闭合 0C/0H/0M）；Action 状态事件权威已收敛：无公开裸 CAS 入口，`cas_transition_for_intent` 为私有机械 primitive，`append_active_event_v2` 与 `PendingActiveEventV2` 已 crate-private，每个成功状态边恰好一条 `action.state_changed` | Schema/typed conformance + root v2 + Action CAS/intent + PD/PolicyRule/五模式评估 + Approval/Identity/Lease/Stop Fence tests | 不从v1迁数据；旧库reinitialize-required |
